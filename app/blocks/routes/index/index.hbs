{{#unless isGameOver}}

    {{rover-controls
        rover=rover
    }}

    {{viewport-block
        rover=rover
        plateau=plateau
    }}

    {{rover-stats
        rover=rover
    }}

{{else}}

    {{game-over-screen
        restart=(action "restart")
    }}

{{/unless}}
