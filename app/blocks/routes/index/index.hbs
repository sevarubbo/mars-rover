{{#unless isGameOver}}

    {{rover-controls
        rover=rover
    }}

    {{viewport-block
        rover=rover
        plateau=plateau
    }}

{{else}}

    {{game-over-screen
        restart=(action "restart")
    }}

{{/unless}}
